---
- hosts: pis
  become: true
  gather_facts: yes
  roles:
    - common
    - samba        # your existing Samba role (writeable share on all Pis)
    - nfs_server   # your existing NFS export role (optional but fine)
    - replication  # NEW: sets up keyless SSH + lsyncd one-to-many
